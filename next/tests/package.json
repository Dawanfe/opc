{
  "name": "invite-tests",
  "version": "1.0.0",
  "description": "邀请码功能测试套件",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "tsx tests/invite.test.ts",
    "test:all": "npm run test && npm run test:integration"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
