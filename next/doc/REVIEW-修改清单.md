# WeOPC 修改清单 (Review)

> 基于飞书文档需求整理，请逐项确认后我再动手修改代码。

---

## 1. 侧边栏 (AppSidebar)

### 1.1 导航菜单项文案调整

| 序号 | 当前文案 | 修改为 | 文件 |
|------|---------|--------|------|
| 1 | 首页概览 | **首页概览**（不变） | AppSidebar.tsx |
| 2 | 政策与工位 | **优惠政策与免费工位** | AppSidebar.tsx |
| 3 | 灵工市场 | **需求广场** | AppSidebar.tsx |
| 4 | AI社区活动 | **AI热门活动** | AppSidebar.tsx |
| 5 | 每日情报 | **每日AI新闻** | AppSidebar.tsx |
| 6 | 会员中心 | **会员中心**（不变） | AppSidebar.tsx |

### 1.2 左下角登录区域
- [ ] 未登录状态的默认用户头像：当前是 `UserCircle` 图标，需确认是否需要换成其他样式的默认头像图片

---

## 2. 登录注册 (LoginModal + AuthContext)

### 2.1 登录方式变更
- [ ] 当前：手机号 + 验证码（验证码功能未接通）
- [ ] **改为：手机号 + 密码**（验证码申请下来前的临时方案）
  - 将"验证码"输入框改为"密码"输入框（type="password"）
  - 删除"获取验证码"按钮
  - AuthContext 中 `loginForm` 字段从 `{ phone, code }` 改为 `{ phone, password }`

### 2.2 登录注册样式
- [ ] 需求提到"采用这个版本的登录注册逻辑和样式"，飞书中有截图但无法查看。**请确认：当前的 LoginModal 弹窗样式是否需要大改？还是仅改上述手机号+密码即可？**

### 2.3 微信扫码登录
- [ ] 需求提到"微信扫码登录"，**暂不实现**（需后端支持），后续迭代

---

## 3. 优惠政策与免费工位 (PolicyWorkspace)

### 3.1 标题/副标题
- [ ] 标题：`政策与工位` → **`优惠政策与免费工位`**
- [ ] 副标题：`全国OPC社区政策查询与工位申请` → **`全国OPC社区政策查询，申请免费工位与其他政策福利`**

### 3.2 搜索框
- [ ] 搜索框 placeholder：`搜索社区名称、城市或政策...` → **`搜索社区名称`**
- [ ] 搜索框宽度缩短（当前 `flex-1` 全宽）→ 给一个固定 `max-w` 或去掉 `flex-1`
- [ ] **删除筛选图标**（Filter 图标）

### 3.3 城市筛选按钮
当前：从数据自动提取省份（20个省份），只显示前6个。

改为固定显示以下城市（按**城市**筛选而非省份）：
- [ ] 全部、北京、上海、深圳、杭州、广州、南京、成都、武汉、厦门

> **注意**：需求写的是"程度"，应为"成都"的笔误。当前筛选逻辑是按`省份`字段匹配，改为按`城市`字段匹配。

### 3.4 政策卡片
- [ ] **删除重复的城市名**：当前卡片左上角同时显示`省份`标签和`城市`文字，部分数据省份和城市相同（如"上海/上海"）。改为只显示一个有意义的位置标签。
- [ ] **底部标签改为**：免费工位、算力支持、创业投资（当前已是这三个：工位、算力、投资 → 文案微调确认）

### 3.5 二级详情页（弹窗）
- [ ] 位置标签格式改为 **`上海·闵行区`** 这种"城市.区县"格式
  - 当前显示的是 `省份 · 城市`（如 `上海 · 上海`）
  - 改为：如果城市和区县都有，显示 `城市·区县`；否则只显示 `城市`

---

## 4. 需求广场 (AIGigMarketplace)

### 4.1 标题/副标题
- [ ] 标题：`灵工市场` → **`订单市场`**
- [ ] 副标题：`AI时代的灵活用工平台，连接需求与人才` → **`发布订单或承接任务，优先找到AI时代新机遇`**

### 4.2 路由名称
- [ ] URL 路径：是否从 `/marketplace` 改为 `/orders` 或其他？还是保持 `/marketplace` 不变？

### 4.3 发布订单功能变更
- [ ] **任务类别增加"其他"选项**
  - 当前类别：AI漫剧制作、视频剪辑、AI技术开发
  - 增加：**其他**
- [ ] **发布权限要求**：当前只要求登录即可发布，改为需要是"政策用户"才能发布（需要定义什么是"政策用户"）
- [ ] **发布后提示**：提交后不直接显示在列表中，而是弹窗提示 **「您的订单需求已提交，将由工作人员联系您」**（需审核）

### 4.4 发布按钮文案
- [ ] 当前：`发布订单` → 确认是否需要改文案（需求里叫"发布订单"，与当前一致）

---

## 5. AI热门活动 (Events)

### 5.1 标题/副标题
- [ ] 标题：`AI社区活动` → **`AI社区活动`**（需求文案写的是"AI社区活动"不变，但侧边栏改为了"AI热门活动"）
- [ ] 副标题：`参与线上线下活动，拓展人脉，获取最新行业资讯` → **`参加线上线下AI活动，认识更多同道中人，获取最新行业资讯`**

### 5.2 卡片 hover 效果
- [ ] 当前 hover 效果：`hover:border-gray-200 hover:shadow-lg`（边框加阴影，无黑色半透明）
- [ ] 需求："所有卡片的hover态不要加黑色半透明，要与其他模块的视觉规范保持一致"
- [ ] **当前实现已经没有黑色半透明**，hover 效果是加深边框+阴影。请确认是否需要调整。

### 5.3 删除指定活动
- [ ] 从 `events.json` 和 `opcData.json` 中删除：**「基于AI大模型软件研发思想与技术公开课」**（id=5，组织者：企赢培训，商业付费活动）

### 5.4 增加联盟活动
- [ ] 需求提到"增加联盟活动，待可欣整理"—— **暂不处理**，等数据提供后再加

---

## 6. 每日AI新闻 (DailyNews)

### 6.1 标题/副标题
- [ ] 确认是否需要调整（当前：标题 `每日情报`，副标题 `OPC与AI领域每日最新政策、新闻与分析`）
- [ ] 侧边栏名称改为"每日AI新闻"，页面标题是否也要同步改为 **`每日AI新闻`**？

### 6.2 删除抖音渠道内容
- [ ] 从 `opcData.json` 的 news 数组中删除所有 `媒体平台 === "抖音"` 的条目（当前有 3 条）

### 6.3 内容刷新
- [ ] 需求提到"具体内容不太行需要刷新一遍"—— **暂不处理**，需提供新数据源

---

## 7. 首页 (Dashboard)

- [ ] 需求截图中有首页模块，但无具体修改说明。**请确认首页是否有需要调整的地方？**

---

## 8. 用户中心 (Profile)

- [ ] 需求列了"用户中心"模块但无具体修改描述。**请确认会员中心是否有需要调整的地方？**

---

## 9. 基本信息（暂不涉及代码）

以下信息记录备忘，暂不需要代码修改：
- 临时网址：globalaialumni.com/weopc
- 正式网址：weopc.com.cn
- 短信服务：待申请
- 微信扫码登录：需后端支持，后续实现

---

## 修改优先级建议

| 优先级 | 模块 | 修改内容 |
|--------|------|----------|
| P0 | 侧边栏 | 导航菜单文案修改（6项） |
| P0 | 登录注册 | 验证码改密码 |
| P0 | 政策与工位 | 标题/副标题/搜索框/城市筛选/卡片/详情页 |
| P0 | 需求广场 | 标题/副标题/增加"其他"类别/发布后审核提示 |
| P0 | AI活动 | 副标题/删除付费活动 |
| P0 | 每日新闻 | 删除抖音内容 |
| P1 | 侧边栏 | 左下角默认用户头像 |
| P2 | 数据 | 活动/新闻内容刷新（等新数据） |
| P2 | 功能 | 微信扫码登录（等后端） |
